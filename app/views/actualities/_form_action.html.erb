<div class="span12">
  <div class="graybox">
    <h1><%= t('.actuality_administration') %></h1>
    <div class="form-actions">
      <%= link_to t('shared.update'),
                  edit_project_actuality_path(project, actuality),
                  class: 'btn' %>
      <%= link_to t('shared.delete'),
                  project_actuality_path(project, actuality),
                  method: 'delete',
                  data: { confirm: t('shared.are_you_sure') },
                  class: 'btn btn-danger' %>
    </div>
  </div>
</div>
