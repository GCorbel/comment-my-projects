comment_partial = '<%= escape_javascript(
  render(@comment, item: @comment.item, new_comment: @comment)
) %>'
<% if @comment.parent %>
  $("#answer_<%= @comment.parent.id %>").html(comment_partial);
<% else %>
  $("#new_root_comment").before(comment_partial);
<% end %>
