$("#answer_<%= @comment.ancestry %>").html('<%= escape_javascript(
  render('new', item: @comment.item, comment: @comment)
) %>');
$('.cancel_comment').on('click', function() {
  $('#answer_<%= @comment.ancestry %>').html('');
})
initMarkdown('<%= @comment.parent_id %>');
