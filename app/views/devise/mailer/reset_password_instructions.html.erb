<% username = @resource.to_s %>
<% link_name = t('.reset_my_password') %>
<% url = edit_password_url(@resource, reset_password_token: @resource.reset_password_token) %>
<%= raw(t('.message', username: username, link: link_to(link_name, url))) %>
