<%= simple_form_for(resource,
                    as: resource_name,
                    url: url,
                    html: { method: :post }) do |f| %>
  <%= f.input :login, required: true %>
  <%= f.submit t('shared.submit') %>
<% end %>

<%= render "links" %>
