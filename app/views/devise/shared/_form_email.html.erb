<%= simple_form_for(resource,
                    as: resource_name,
                    url: url,
                    html: { method: :post }) do |f| %>
  <%= f.input :email %>
  <%= f.submit "Envoyer" %>
<% end %>

<%= render "links" %>
