<div class="row-fluid">
  <div class="span12">
    <div class="graybox">
      <h1><%= link_to project, project %></h1>
      <p><%= markdown(excerpt_for(project, text)) %></p>
      <%= raw(tags_for(project)) %>
    </div>
  </div>
</div>
